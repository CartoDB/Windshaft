sudo: required
dist: trusty

services:
  - docker

before_install:
  - docker pull cartoimages/windshaft-testing

script:
  - docker run -v `pwd`:/srv cartoimages/windshaft-testing

language: generic
